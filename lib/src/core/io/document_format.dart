/// Supported document formats for loading and saving.
enum DocumentFormat {
  /// The human-readable .markdraw format.
  markdraw,

  /// The Excalidraw JSON format (.excalidraw or .json).
  excalidraw,
}
